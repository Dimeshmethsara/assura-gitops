apiVersion: v1
kind: Service
metadata:
  name: assura-frontend
  labels:
    {{- include "assura-frontend.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "assura-frontend.labels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
