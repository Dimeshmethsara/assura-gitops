apiVersion: v1
kind: Service
metadata:
  name: assura-backend
  labels:
    {{- include "assura-backend.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "assura-backend.labels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
