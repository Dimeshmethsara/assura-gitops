title = "assura-gitops gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Placeholder ARNs (CHANGE_ME/187691954427) are not real secrets; real values never live in this repo — see README.md"
regexes = [
  '''187691954427''',
  '''CHANGE_ME''',
]
